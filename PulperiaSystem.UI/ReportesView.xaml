<UserControl x:Class="PulperiaSystem.UI.ReportesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900" Background="White">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Filtros -->
            <RowDefinition Height="Auto"/> <!-- Cards -->
            <RowDefinition Height="*"/>    <!-- DataGrid -->
        </Grid.RowDefinitions>

        <!-- Filtros -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Desde:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <DatePicker x:Name="dpInicio" Width="120" Margin="0,0,15,0"/>
            
            <TextBlock Text="Hasta:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <DatePicker x:Name="dpFin" Width="120" Margin="0,0,15,0"/>

            <TextBlock Text="Cajero:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <ComboBox x:Name="cmbCajeros" Width="150" Margin="0,0,15,0" DisplayMemberPath="NombreUsuario" SelectedValuePath="UsuarioId"/>

            <Button Content="🔍 Generar Reporte" Width="140" Height="30" Background="#2C3E50" Foreground="White" Click="BtnGenerar_Click"/>
        </StackPanel>

        <!-- Cards Metrics -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Card 1 -->
            <Border Grid.Column="0" Background="#3498DB" CornerRadius="8" Padding="15" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Ventas Totales" Foreground="White" Opacity="0.8"/>
                    <TextBlock x:Name="txtVentasTotales" Text="$0.00" Foreground="White" FontSize="24" FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <!-- Card 2 -->
            <Border Grid.Column="1" Background="#27AE60" CornerRadius="8" Padding="15" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Ganancia Estimada" Foreground="White" Opacity="0.8"/>
                    <TextBlock x:Name="txtGanancia" Text="$0.00" Foreground="White" FontSize="24" FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <!-- Card 3 -->
            <Border Grid.Column="2" Background="#E67E22" CornerRadius="8" Padding="15">
                <StackPanel>
                    <TextBlock Text="Transacciones" Foreground="White" Opacity="0.8"/>
                    <TextBlock x:Name="txtTransacciones" Text="0" Foreground="White" FontSize="24" FontWeight="Bold"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Grid Detalle -->
        <DataGrid x:Name="gridDetalle" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True" 
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                  Background="White" BorderBrush="#EEEEEE" RowHeight="35">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID Venta" Binding="{Binding VentaId}" Width="60"/>
                <DataGridTextColumn Header="Cajero" Binding="{Binding Usuario}" Width="100"/>
                <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha, StringFormat='dd/MM/yyyy HH:mm'}" Width="120"/>
                <DataGridTextColumn Header="Producto" Binding="{Binding Producto}" Width="*"/>
                <DataGridTextColumn Header="Cant" Binding="{Binding Cantidad}" Width="60"/>
                <DataGridTextColumn Header="Precio Unit." Binding="{Binding Precio, StringFormat=C}" Width="80"/>
                <DataGridTextColumn Header="Subtotal" Binding="{Binding Subtotal, StringFormat=C}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
