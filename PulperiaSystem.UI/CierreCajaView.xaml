<UserControl x:Class="PulperiaSystem.UI.CierreCajaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900" Background="White">

    <Grid>
        <Border Width="500" CornerRadius="10" Background="White">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.2"/>
            </Border.Effect>
            <Grid Margin="30">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="CORTE DE CAJA (Z)" FontSize="24" FontWeight="Bold" Foreground="#C0392B" HorizontalAlignment="Center" Margin="0,0,0,30"/>

                <!-- Resumen Sistema -->
                <StackPanel Grid.Row="1" Margin="0,0,0,30">
                    <TextBlock Text="Resumen del Sistema" FontWeight="Bold" Foreground="#555" Margin="0,0,0,10"/>
                    <Border BorderBrush="#EEE" BorderThickness="1" CornerRadius="5" Padding="15">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/> 
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="Inicio de Turno:" Foreground="Gray"/>
                            <TextBlock x:Name="lblFechaInicio" Grid.Column="1" FontWeight="SemiBold"/>

                            <Separator Grid.Row="1" Grid.ColumnSpan="2" Margin="0,10"/>

                            <TextBlock Grid.Row="2" Text="Fondo Inicial:" Foreground="#333"/>
                            <TextBlock Grid.Row="2" x:Name="lblMontoInicial" Grid.Column="1" Text="$0.00" Foreground="#333"/>

                            <TextBlock Grid.Row="3" Text="+ Ventas (Efectivo):" Foreground="Green" Margin="0,5,0,0"/>
                            <TextBlock Grid.Row="3" x:Name="lblVentas" Grid.Column="1" Text="$0.00" Foreground="Green" Margin="0,5,0,0"/>

                            <TextBlock Grid.Row="4" Text="= TOTAL ESPERADO:" FontWeight="Bold" Foreground="#333" Margin="0,15,0,0" FontSize="16"/>
                            <TextBlock Grid.Row="4" x:Name="lblTotalEsperado" Grid.Column="1" Text="$0.00" FontWeight="Bold" Foreground="#333" Margin="0,15,0,0" FontSize="16"/>
                        </Grid>
                    </Border>
                </StackPanel>

                <!-- Corte Ciego -->
                <StackPanel Grid.Row="2" Margin="0,0,0,30">
                    <TextBlock Text="Conteo Físico (Lo que tienes en mano)" FontWeight="Bold" Foreground="#2980B9" Margin="0,0,0,10"/>
                    <TextBox x:Name="txtMontoReal" Height="50" FontSize="22" FontWeight="Bold" TextAlignment="Center" VerticalContentAlignment="Center" Background="#ECF0F1" BorderThickness="0"/>
                </StackPanel>

                <!-- Botón -->
                <Button Grid.Row="3" Content="CERRAR TURNO" Height="50" Background="#C0392B" Foreground="White" FontSize="16" FontWeight="Bold" Click="BtnCerrarTurno_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border CornerRadius="5" Background="{TemplateBinding Background}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Border>
    </Grid>
</UserControl>
